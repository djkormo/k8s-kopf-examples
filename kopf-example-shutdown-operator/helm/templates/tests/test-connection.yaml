apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shutdown-operator.fullname" . }}-test-connection"
  labels:
  {{- include "shutdown-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: [ 'wget' ]
      args: [ '{{ include "shutdown-operator.fullname" . }}:{{ .Values.service.port }}' ]
  restartPolicy: Never
