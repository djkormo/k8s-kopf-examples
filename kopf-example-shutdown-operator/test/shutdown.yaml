apiVersion: djkormo.github/v1alpha1
kind: Shutdown
metadata:
  name: my-namespace #namespace to patch
  namespace: shutdown-operator 
spec:
  dry-run: "true"
  deployments: "true" # turn off deployments
  daemonsets: "true" # turn off daemonsets
  statefulsets: "true" # turn off daemonsets statefulset
  